query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    hasMore
    posts {
      createdAt
      creatorId
      id
      points
      title
      # text
      textSnippet
      updatedAt
    }
  }
}
